# AGENTS.md

## 基础
目标：在保证最终交付质量的前提下，减少流程性审查、重复验证、无意义哈希检查和阻塞式门禁。请优先完成有效实现，不要机械套用重型开发流程。

- 用中文进行对话回复
- 运行代码时，轮询时间改为15min而不是30s，以节省token用量。
- 禁止无意义的频繁哈希。
- 在虚拟环境中安装所需依赖，联网检索，运行长时间训练、评估或参数搜索，执行 Git 操作，不需要再次单独征求许可。
- 编码前先理清思路：遇到不确定的内容主动提问，不要凭空猜测；存在多种解读方向时逐一列出；如果发现有更简洁高效的实现方案，主动提出建议。
- 坚持简洁优先原则：用最少代码实现需求，不为临时一次性功能搭建复杂抽象层，不必为极低概率的场景编写容错逻辑；冗余代码及时精简，不要写假设情况下的兜底检查代码。
- 只修改必要内容：绝不顺手重构运行正常的代码，不改动无关代码与原有注释；发现无效代码可以标注提醒，不要直接删除。
- 以目标为导向执行：先明确任务完成标准，再着手操作；修复、新增、重构类任务，优先设计验证方案，循环自检直至达标。

## 总体原则

- 不采用“每个子任务固定进行规格审查 + 质量审查”的双审模式。
- 取消每个小任务、小修复之后的阻塞式质量门禁。
- 开发阶段以局部验证为主，里程碑和最终交付时才进行全量验证。
- 验证强度必须与改动风险匹配，不得因为流程模板而重复执行相同命令。
- 不要为了证明自己认真而增加额外流程。
- 所有审查、测试、构建、哈希检查都必须能够说明它具体在防止什么风险。

## 审查策略

- 默认由实现代理完成自检，不再为每个子任务分别启动“规格审查代理”和“质量审查代理”。
- 仅在以下情况启动独立审查代理：修改核心架构、公共接口、权限、安全、数据迁移或持久化逻辑；改动跨越多个模块，局部影响难以判断；连续两次修复仍未解决同一问题；即将完成一个明确里程碑；最终交付前进行一次整体审查。
- 普通测试补充、局部函数修复、单文件或少量文件修改，不启动双重审查。
- 审查采用批量方式：每完成约 3–4 个相关子任务，再进行一次合并审查；


## 本项目相关
- 方法和模块的详细信息，包括模块功能和方法、相关公式、输入输出等要动态维护在README_CN.md 中。
- 虚拟环境固定为 conda activate /mnt/nas1/disk09/yuejianwu/.conda/envs/biocoop，gpu按每次任务一张卡进行分配。
- 每次运行实验需要记录在note.txt，只需简单记录输出目录、对应命令行、当前使用的方法，其余信息不需要记录。